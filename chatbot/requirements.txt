# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0

# Pydantic v2
pydantic==2.10.4
pydantic-settings==2.7.0

# Async HTTP
httpx==0.27.2

# Database — async Supabase REST + direct asyncpg
supabase==2.10.0
asyncpg==0.30.0

# Vector store
qdrant-client==1.12.1

# Redis cache
redis==5.2.1

# LLM — Groq uses the OpenAI SDK (same API, different base_url)
openai==1.58.1
# Keep anthropic optional — only needed if LLM_PROVIDER=anthropic
anthropic==0.40.0

# Local embeddings + reranking (free, no API key)
sentence-transformers==3.3.1
# torch is already in venv; listed here to make Docker reproducible
torch==2.5.1

# Cohere reranker (optional, only if RERANKER_PROVIDER=cohere)
cohere==5.13.5

# Background jobs
arq==0.26.1

# Observability
structlog==24.4.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
prometheus-client==0.21.1

# Utilities
python-dotenv==1.0.1
loguru==0.7.3
python-jose[cryptography]==3.3.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Eval
ragas==0.2.10
